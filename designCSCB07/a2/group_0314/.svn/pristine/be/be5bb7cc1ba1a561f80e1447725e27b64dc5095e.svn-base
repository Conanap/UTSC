Backlog item						Estimate
*allow users to create directories (and files) 		1 day
*allow users to remove directories (and files)		1 day
*allow users to copy directories (and files)		1 day
*allow users to rename directories (and files)		1 day
*allow users to move directories (and files) to another	1 day
*allow users to 
